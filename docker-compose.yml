version: '3.8'

services:
  saas-panel:
    build: .
    container_name: saas-panel-v3
    restart: always
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - DB_HOST=postgres
      - DB_PORT=5432
      - DB_USER=postgres
      - DB_PASSWORD=e2zaFkFxxIfX04QxP55LEvX2ZNW6XKFb
      - DB_NAME=saas_delivery
      - JWT_SECRET=super_secret_saas_key
      # IP da sua VPS para o painel se conectar à Evolution API:
      - EVOLUTION_API_URL=http://116.203.152.114:8080
      - EVOLUTION_API_KEY=X8G9W2M4V5N7B3L1K6J0H9P2Y3T5C8F1
    # Se o seu postgres estiver na mesma VPS, você pode conectar eles na mesma rede:
    # networks:
    #   - n8n_network

# networks:
#   n8n_network:
#     external: true
